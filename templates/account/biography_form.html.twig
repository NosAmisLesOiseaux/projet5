{{ form_start(biography_form) }}
    {{ form_label(biography_form.biography) }}
    {{ form_errors(biography_form.biography) }}
    {{ form_widget(biography_form.biography) }}
<button type="submit">Mettre à jour ma biographie</button>
{{ form_end(biography_form) }}